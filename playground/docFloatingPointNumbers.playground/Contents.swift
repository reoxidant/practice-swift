import UIKit

//the number 64 bit get more 15 numbers in the one value and have a type as Double

let numberDouble = Double.greatestFiniteMagnitude // 15 digits in the number

//the number 32 bit get in twelve small number than Double and this type has a name as Float

let numberFloat = Float.greatestFiniteMagnitude // only 6 digits in the number

// in case where need to use both types of numbers, most priority is Double
